import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  goals: {
    flex: 1,
    flexDirection: "column",
    alignItems: "center",
    marginTop: 20
  },
  goal: {
    width: "95%",
    marginTop: 20
  }
});
